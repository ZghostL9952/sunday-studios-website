# Optional: URL for the game trailer video (host Trailer long.mov on YouTube, Vimeo, or a CDN)
# For GitHub Actions deploy, add this as a repository secret and pass it in the build step.
VITE_TRAILER_VIDEO_URL=
